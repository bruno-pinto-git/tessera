# Tessera login theme — inherits Keycloak 26's PatternFly-v5 login theme
# (keycloak.v2) and layers a stylesheet that matches the SPA's dark + green
# look & feel (see frontend/src/index.css).
parent=keycloak.v2
import=common/keycloak
styles=css/styles.css css/tessera.css
