type: object
description: |
  RFC 7807 Problem Details for HTTP APIs. Returned with media type
  `application/problem+json` for all error responses.
required:
  - type
  - title
  - status
properties:
  type:
    type: string
    format: uri
    description: A URI reference identifying the problem type.
    examples:
      - "https://tessera/api/errors/validation"
  title:
    type: string
    description: Short, human-readable summary of the problem type.
    examples:
      - "Validation failed"
  status:
    type: integer
    format: int32
    description: HTTP status code generated by the origin server.
    examples:
      - 400
  detail:
    type: string
    description: Human-readable explanation specific to this occurrence.
    examples:
      - "Field 'kickoffAt' must be in the future."
  instance:
    type: string
    format: uri
    description: URI reference identifying the specific occurrence.
    examples:
      - "/api/v1/matches/42"
  errors:
    type: array
    description: Field-level errors when `type` is a validation problem.
    items:
      type: object
      required:
        - field
        - message
      properties:
        field:
          type: string
          description: Dotted path to the offending field.
          examples:
            - "kickoffAt"
        message:
          type: string
          examples:
            - "must be in the future"
